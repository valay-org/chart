apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - all.yaml

patches:
  - path: delete-job.yaml
    target:
      kind: Job
      name: delete
      namespace: ethernity-setup

