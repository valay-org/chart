apiVersion: v1
kind: Service
metadata:
  name: {{ include "valay.fullname" . }}
  labels:
    {{- include "valay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "valay.selectorLabels" . | nindent 4 }}
