apiVersion: v1
kind: Service
metadata:
  name: {{ include "valay-authentication.fullname" . }}
  labels:
    {{- include "valay-authentication.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "valay-authentication.selectorLabels" . | nindent 4 }}
