apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "valay-authentication.fullname" . }}-test-connection"
  labels:
    {{- include "valay-authentication.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "valay-authentication.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
