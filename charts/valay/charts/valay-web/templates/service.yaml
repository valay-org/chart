apiVersion: v1
kind: Service
metadata:
  name: {{ include "valay-web.fullname" . }}
  labels:
    {{- include "valay-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "valay-web.selectorLabels" . | nindent 4 }}
