apiVersion: v1
kind: Service
metadata:
  name: {{ include "valay-user.fullname" . }}
  labels:
    {{- include "valay-user.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "valay-user.selectorLabels" . | nindent 4 }}
